# Empty value uses the Vite /api proxy in local development.
VITE_API_BASE_URL=/api/v1
VITE_API_PROXY_TARGET=http://localhost:8000
VITE_DEMO_MODE=false
# Optional decorative WebGL pipeline. Disabled by default to avoid an ~823 KB lazy bundle.
VITE_PIPELINE_3D=false
