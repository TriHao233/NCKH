CẤU TRÚC: dien_khuyet
1. `question` có đúng một chỗ trống `_____`; khi điền đáp án phải thành câu hoàn chỉnh và đủ dữ kiện để suy ra duy nhất một đáp án.
2. Chỗ trống là thuật ngữ, thuộc tính hoặc kết quả quan trọng.
3. `options` là `null`; `correct_answer` là từ/cụm từ cần điền, đủ cụ thể để chấm tự động.
