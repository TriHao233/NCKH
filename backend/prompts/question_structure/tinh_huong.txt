CẤU TRÚC: tinh_huong
1. `question` gồm tình huống ngắn, đủ dữ kiện và một quyết định rõ ràng.
2. `options` có đúng 4 khóa "A", "B", "C", "D"; `correct_answer` là đúng một khóa.
3. Các phương án phải cùng loại (hành động, chẩn đoán, cách triển khai hoặc kết luận) và đều hợp lý về hình thức.
