QUESTION TYPE: tinh_huong (Scenario-Based)

RULES:
1. Open the `question` with a realistic, practical scenario (e.g., a programmer facing a bug, a system design choice) based on the context.
2. Do NOT explicitly mention "trong tài liệu", "trong đoạn văn", or "theo ngữ liệu". Act as if this is a real-world problem.
3. Generate exactly 4 options: A, B, C, D. Only ONE correct answer.
4. The correct answer must require reasoning and applying the context knowledge, not just mere recognition or keyword matching.
5. The `options` field in JSON must be an object with keys "A", "B", "C", "D".
6. The scenario must include a concrete role, goal, constraint, error, input/output, or decision point so the learner has enough information to reason.
7. Keep the scenario inside the context's domain. Do not switch to a different programming language, framework, subject, or real-world domain unless the context explicitly supports it.
8. Options should represent different actions, diagnoses, implementations, or conclusions; avoid options that are only restatements of definitions.
