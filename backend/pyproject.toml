[tool.ruff]
target-version = "py310"
line-length = 120
exclude = ["modules/ocr", "modules/rag"]

[tool.ruff.lint]
# Start with correctness rules that are safe to enforce on the existing codebase.
select = ["E9", "F63", "F7", "F82"]
