APP_NAME=QBankCTU API
APP_VERSION=0.2.0
APP_ENV=development
# Demo login is disabled unless explicitly enabled.
DEMO_MODE=false
# Set these only in a local/demo environment when DEMO_MODE=true.
DEMO_ADMIN_EMAIL=admin@qbankctu.edu.vn
DEMO_ADMIN_PASSWORD=
DEMO_REVIEWER_EMAIL=reviewer@qbankctu.edu.vn
DEMO_REVIEWER_PASSWORD=
HOST=0.0.0.0
PORT=8000

MONGO_URI=mongodb+srv://user:password@cluster.example.mongodb.net/
AUTH_DB_NAME=NCKH
RAG_DB_NAME=rag_database
MONGO_CONNECT_TIMEOUT_MS=10000
# Production/staging must use a replica set so multi-document writes stay atomic.
REQUIRE_MONGO_TRANSACTIONS=false
JOB_RECOVERY_TIMEOUT_MINUTES=120
JOB_WORKER_POLL_SECONDS=1
JOB_LEASE_SECONDS=120
JOB_HEARTBEAT_SECONDS=30
JOB_MAX_ATTEMPTS=3
JOB_RETRY_BASE_SECONDS=5
JOB_RETRY_MAX_SECONDS=300
MAX_ACTIVE_GENERATION_JOBS_PER_USER=10
WORKER_SHUTDOWN_GRACE_SECONDS=30
JOB_RETENTION_DAYS=90
LLM_SLOT_LEASE_SECONDS=90
LLM_SLOT_HEARTBEAT_SECONDS=15
LLM_SLOT_WAIT_TIMEOUT_SECONDS=600
LLM_SLOT_POLL_SECONDS=0.5
OLLAMA_MAX_CONCURRENCY=1
GEMINI_MAX_CONCURRENCY=5
REVIEW_LOCK_TIMEOUT_MINUTES=30
# Acceptance defaults to local inference. CLOUD_ALLOWED must be an explicit environment decision.
INFERENCE_POLICY=LOCAL_ONLY
LOCAL_INFERENCE_ALLOWED_HOSTS=localhost,127.0.0.1,::1,host.docker.internal,ollama

FIREBASE_CREDENTIALS_PATH=./firebase-service-account.json

CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
ALLOWED_HOSTS=localhost,127.0.0.1,testserver
API_PREFIX=/api/v1

MODEL_PROVIDER=qwen
CODE_GENERATION_MODEL_PROVIDER=deepseek
EVALUATION_MODEL_PROVIDER=qwen
EVALUATION_NUM_PREDICT=900
GENERATION_FALLBACK_PROVIDER=
EVALUATION_FALLBACK_PROVIDER=
OLLAMA_GENERATE_URL=http://localhost:11434/api/generate
OLLAMA_TIMEOUT_SECONDS=300
OLLAMA_NUM_PREDICT=900
OLLAMA_TEMPERATURE=0
QWEN_MODEL_NAME=qwen2.5:7b
DEEPSEEK_MODEL_NAME=deepseek-r1
GEMINI_API_KEY=
GEMINI_MODEL_NAME=gemini-2.0-flash
# Use Qwen as the evaluator provider.
# EVALUATION_MODEL_PROVIDER=qwen
# Use any Ollama model for evaluator with the ollama:<model-name> form.
# EVALUATION_MODEL_PROVIDER=ollama:qwen2.5:7b
PROMPT_SOURCE=file
CHROMADB_COLLECTION_NAME=chunks
EMBEDDING_MODEL_NAME=all-MiniLM-L6-v2
CHROMADB_PATH=./data/chroma_data
UPLOAD_DIR=./data/uploads
OCR_OUTPUT_DIR=./data/ocr_outputs
# Optional on Windows when Poppler is not in PATH.
# POPPLER_PATH=C:\poppler\Library\bin
